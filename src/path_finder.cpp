#include "path_finder.hpp"

#include <queue>

using namespace org::kata;

std::vector<int> path_finder::dijsktra(adj_list G, int src, int dst) {
    std::queue<int> next;
}

std::unordered_map<int, int> path_finder::sssp(adj_list G, int src) {

}