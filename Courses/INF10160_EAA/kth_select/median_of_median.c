/*
 * Median-of-Median algorithm.
 * Worst case: O(n)
 * Best case: O(n)
 */
#include <math.h>

#define ANCHOR_LEN 5

int __Split(int *A, int l, int r)
{
  return 0;
}

int __Median(int *A, int l, int r)
{
  return l;
}

int MedianOfMedian(int *A, int l, int r, int k)
{
  if (l - r <= ANCHOR_LEN) {
  }

  return 0;
}
 
